/**
 * Write a function that takes a non-negative integer n and prints the numbers 0 through n in a clockwise spiral, 
 * starting at the top-left of a square grid. Hint: The grid size should be ceil(sqrt(n + 1)), 
 * and any unused cells should be blank.
 * 
 */

function spiralGrid (){

}

console.log(30);